Place your profile image here as `profile.jpg` so the About section uses it.

- Recommended: 1200x1600 or 600x800 JPG/PNG.
- Filename must be `profile.jpg` (path: /profile.jpg).

Optional: add a `Sarim_CV.pdf` file in `public/` to enable the Download CV button.